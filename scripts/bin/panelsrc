#!/bin/sh
# bar settings

export PANEL_FONT="tewi:size=9"
export PANEL_BG="$(xrq color0)"
export PANEL_FG="$(xrq foreground)"
#export BORD_COL=""
export PANEL_OPTS="-d -f "$PANEL_FONT" -B "$PANEL_BG" -F "$PANEL_FG" -g 1920x20+0+0"
