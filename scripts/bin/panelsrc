#!/bin/sh
# bar settings

export PANEL_FONT="LuxiMono:size=11"
export PANEL_BG="$(xrq color0)"
export PANEL_FG="$(xrq foreground)"
export PANEL_OPTS="-d -f "$PANEL_FONT" -B "$PANEL_BG" -F "$PANEL_FG" -g 180x24+70+3" 
