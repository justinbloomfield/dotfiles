#!/bin/sh
LIBC=${1:-musl}
PKG=$2

cd $VPKG_DIR &&

if [ "$LIBC" = "musl" ]; then 
	./xbps-src pkg -m masterdir-x86_64-musl "$PKG"
elif [ "$LIBC" = "glibc" ]; then
	./xbps-src pkg -m masterdir "$PKG"
fi 

sudo xbps-install --repository=hostdir/binpkgs "$PKG"
