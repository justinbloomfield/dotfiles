#!/bin/sh
DIR=${SRC_DIR:-/home/$(whoami)/src/pkg}
SELF_DIR=$(pwd)/

isExist(){
	[ -e "$1" ] && return 0 || return 1
}

if ( isExist "$1" )
then
	ln -st ${DIR}/$1/ $(pwd)/$1/* 
else
	echo "File doesn't exist"
fi


