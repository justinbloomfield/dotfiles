#!/bin/sh
# requires wmutils-core to use like this (dynamic centering)
ROOT_ID = "$(lsw -r)"
ROOT_WIDTH = "$(wattr w ${ROOT_ID})"
while true; do
	echo "%{c}$(date "+%H:%M:%S, %A, %B %d")" 
	sleep 1 
done | lemonbar -g $ROOT_WIDTHx25+0+5 -d -f metis -F "#ffffff" -B "#000000" #-R "#ffffff" -r 2
