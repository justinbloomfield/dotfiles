#!/bin/sh

FREQ=${FREQ:-0.5}

while :; do
    COLORS=$(tac < ~/.config/wmrc/colors | cat - ~/.config/wmrc/colors | tr -d '#')
    for c in $COLORS; do
        CUR=$(pfw)
        test "`wattr wh $CUR`" != "`wattr wh $(lsw -r)`" && chwb -c $c $CUR
        sleep $FREQ
    done
done
